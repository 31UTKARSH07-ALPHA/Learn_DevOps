{
  "name": "nodejs-deployment",
  "version": "1.0.0",
  "private": true,
  "main": "app.js",
  "scripts": {
    "build": "esbuild app.js --bundle --platform=node --target=node20 --minify --outfile=dist/app.js",
    "start": "node dist/app.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
